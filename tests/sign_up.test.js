/* 
 * Contact: team@fusioncode.ai
 *
 *
 * Project  - sign up UNIT TESTS
 * Application Client
 * single service
*/

/* the sign up view has a profile icon located at the very-top center of the view which on click will trigger the profile() action */
it("the sign up view has a profile icon located at the very-top center of the view which on click will trigger the profile() action", () => {
  /* ToDo developer needs to complete unit test 
               the sign up view has a profile icon located at the very-top center of the view which on click will trigger the profile() action
               uncomment below and update the code to test
                //instance.action(function(error) {
                //  if (error) throw error;
                //expect().to.be();
                //});
                done();
               */
})
