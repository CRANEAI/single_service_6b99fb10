/* 
 * Contact: team@fusioncode.ai
 *
 *
 * Project  - following UNIT TESTS
 * Application Client
 * single service
*/

/* the following view has a profile icon located at the very-top center of the view which on click will trigger the profile() action */
it("the following view has a profile icon located at the very-top center of the view which on click will trigger the profile() action", () => {
  /* ToDo developer needs to complete unit test 
               the following view has a profile icon located at the very-top center of the view which on click will trigger the profile() action
               uncomment below and update the code to test
                //instance.action(function(error) {
                //  if (error) throw error;
                //expect().to.be();
                //});
                done();
               */
})
